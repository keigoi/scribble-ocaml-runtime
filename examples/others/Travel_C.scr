
module travel.Travel_Travel_C;
type <java> java.lang.Integer from java.lang.Integer as Int;
type <java> java.lang.String from java.lang.String as String;
local protocol Travel_C(role C, role A, role S) {

	choice at C {
	
		Query(String) to A;
		Quote(Int) from A;
		do travel.Travel_Travel_C.Travel_C(C, A, S);
		
	} or {
	
		choice at C {
		
			Yes() to A;
			Payment(String) to S;
			Ack() from S;
			
		} or {
		
			No() to A;
			
		}
		
    Bye() to A;
	}
}

(jbuild_version 1)

(executable
 ((name oauth_server)
  (preprocess (pps (lwt.ppx linocaml.ppx linocaml.ppx_lens)))
  (flags ("-g" "-short-paths" "-ppx" "`ocamlfind query ppx_implicits`/ppx_implicits"))
  ; (preprocess (pps (linocaml.ppx linocaml.ppx_lens)))
  ; (flags ("-g" "-short-paths" "-ppx" "`ocamlfind query `" "-ppx" "`ocamlfind query ppx_implicits`/ppx_implicits"))
  (libraries (scribble.lwt linocaml.lwt conduit-lwt-unix cohttp-lwt-unix uri sexplib cmdliner))))

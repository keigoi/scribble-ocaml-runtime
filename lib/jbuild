(jbuild_version 1)

(library
 ((name scribble)
  (public_name scribble)
  ; (flags ("-leopard"))
  (flags ("-ppx" "`ocamlfind query ppx_implicits`/ppx_implicits"))
  (libraries (threads linocaml ppx_implicits.runtime))))
